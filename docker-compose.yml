version: "3.9"
services:
  tabio:
    container_name: "Tabio"
    ports:
      - '80:80'
    volumes:
      - './tabio/models:/app/tabio/models'
    environment:
      - LOG_LEVEL=info
      - WORKERS_PER_CORE=4
    build:
      context: ./
      dockerfile: Dockerfile
